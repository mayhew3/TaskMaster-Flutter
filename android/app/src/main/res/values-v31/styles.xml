<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The Flutter app forces ThemeMode.dark regardless of system setting,
         so the v31 day variant uses the same Theme.Black.NoTitleBar parent
         as values-night-v31/styles.xml. This avoids a light/white flash on
         Android 12+ between the OS splash and Flutter's first frame when
         the system is in light mode. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#2B72C2</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
        <item name="android:windowSplashScreenIconBackgroundColor">#2B72C2</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
